import React, { useEffect, useState } from "react";

const Analyses: React.FC = () => {

    useEffect(() => {}, [])

    return (
        <>
            <h1>Analyses</h1>
        </>
    );
};

export default Analyses;
